import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import logo from "../../assets/sankalp-logo.png";
import { logoutAdmin } from "../admin/admin-dashboard.service";
import { createCaseHistory, fetchDoctorAppointment, fetchDoctorAppointments, fetchPatientHistory, updateCaseHistory } from "./doctor-appointments.service";
import { CaseHistoryForm, DoctorAppointment, DoctorAppointmentDetail, PatientHistory } from "./doctor-appointments.types";
import "./DoctorDashboard.css";

const emptyForm: CaseHistoryForm = { appointmentDate: "", nextAppointmentDate: "", presentingConcern: "", medicalHistory: "", assessment: "", treatmentProvided: "", therapyGoals: "", progressNotes: "", homeProgram: "", recommendations: "", caseHistory: "", additionalNotes: "" };
const inputDate = (date?: string | null) => date ? date.split("-").reverse().join("-") : "";
const fromHistory = (value: NonNullable<DoctorAppointmentDetail["caseHistory"]>): CaseHistoryForm => ({ appointmentDate: inputDate(value.appointmentDate), nextAppointmentDate: inputDate(value.nextAppointmentDate), presentingConcern: value.presentingConcern ?? "", medicalHistory: value.medicalHistory ?? "", assessment: value.assessment ?? "", treatmentProvided: value.treatmentProvided ?? "", therapyGoals: value.therapyGoals ?? "", progressNotes: value.progressNotes ?? "", homeProgram: value.homeProgram ?? "", recommendations: value.recommendations ?? "", caseHistory: value.caseHistory, additionalNotes: value.additionalNotes ?? "" });

export const DoctorDashboard = () => {
  const navigate = useNavigate(); const [items, setItems] = useState<DoctorAppointment[]>([]); const [selected, setSelected] = useState<DoctorAppointmentDetail | null>(null); const [history, setHistory] = useState<PatientHistory | null>(null); const [view, setView] = useState<"TODAY" | "PREVIOUS" | "ALL">("TODAY"); const [form, setForm] = useState<CaseHistoryForm>(emptyForm); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [menu, setMenu] = useState(false);
  const todayDisplay = useMemo(() => new Date().toLocaleDateString("en-GB").replaceAll("/", "-"), []); const todayInput = useMemo(() => new Date().toLocaleDateString("en-CA"), []);
  const handleError = useCallback((reason: unknown) => { if (reason instanceof Error && reason.message === "SESSION_INVALID") navigate("/login", { replace: true }); else setError(reason instanceof Error ? reason.message : "Something went wrong."); }, [navigate]);
  const load = useCallback(async () => { setLoading(true); try { setItems(await fetchDoctorAppointments()); } catch (reason) { handleError(reason); } finally { setLoading(false); } }, [handleError]);
  useEffect(() => { void load(); }, [load]);
  const visible = items.filter((item) => { if (view === "ALL") return true; if (view === "TODAY") return item.scheduledDate === todayDisplay; if (!item.scheduledDate) return false; const [day, month, year] = item.scheduledDate.split("-").map(Number); return new Date(year, month - 1, day).getTime() < new Date().setHours(0, 0, 0, 0); });
  const select = async (item: DoctorAppointment) => { setError(""); setNotice(""); try { const [detail, previous] = await Promise.all([fetchDoctorAppointment(item.referenceId), fetchPatientHistory(item.patientId)]); setSelected(detail); setHistory(previous); setForm(detail.caseHistory ? fromHistory(detail.caseHistory) : { ...emptyForm, appointmentDate: inputDate(detail.scheduledDate) || todayInput }); } catch (reason) { handleError(reason); } };
  const save = async (event: FormEvent) => { event.preventDefault(); if (!selected) return; setBusy(true); setError(""); setNotice(""); try { if (selected.caseHistory) await updateCaseHistory(selected.caseHistory.id, form); else await createCaseHistory(selected.referenceId, form); const detail = await fetchDoctorAppointment(selected.referenceId); setSelected(detail); setForm(detail.caseHistory ? fromHistory(detail.caseHistory) : form); setHistory(await fetchPatientHistory(selected.patientId)); setNotice("Patient case history saved successfully."); await load(); } catch (reason) { handleError(reason); } finally { setBusy(false); } };
  const logout = async () => { await logoutAdmin(); navigate("/login", { replace: true }); };
  const fields: Array<[keyof CaseHistoryForm, string, string]> = [["presentingConcern", "Presenting Concern", "What brought the child for care?"], ["medicalHistory", "Medical History", "Relevant medical and developmental history"], ["assessment", "Clinical Assessment", "Observations and assessment findings"], ["treatmentProvided", "Treatment Provided", "Therapy completed during this visit"], ["therapyGoals", "Therapy Goals", "Short and long-term goals"], ["progressNotes", "Progress Notes", "Response and progress observed"], ["homeProgram", "Home Program", "Activities advised for home"], ["recommendations", "Recommendations", "Further clinical recommendations"]];
  return <div className="doctor-dashboard-shell"><button className="doctor-menu-button" type="button" onClick={() => setMenu(!menu)}>☰ <span>Menu</span></button><aside className={`doctor-sidebar${menu ? " is-open" : ""}`}><img src={logo} alt="Sankalp" /><nav><button className="is-active" type="button">▦ Dashboard</button><button type="button" onClick={() => setView("TODAY")}>📅 Today’s Patients</button><button type="button" onClick={() => setView("PREVIOUS")}>📖 Patient History</button></nav><button className="doctor-logout" type="button" onClick={() => void logout()}>↪ Logout</button></aside><main className="doctor-dashboard"><header><div><small>DOCTOR WORKSPACE</small><h1>My Patient Schedule</h1><p>Review appointments and keep each child’s clinical journey up to date.</p></div><span className="doctor-profile">🧑‍⚕️ <b>Doctor</b></span></header>{error && <p className="doctor-alert doctor-alert--error" role="alert">{error}</p>}{notice && <p className="doctor-alert doctor-alert--success" role="status">{notice}</p>}<nav className="doctor-tabs">{(["TODAY", "PREVIOUS", "ALL"] as const).map((value) => <button className={view === value ? "is-active" : ""} type="button" onClick={() => setView(value)} key={value}>{value === "TODAY" ? "Today" : value === "PREVIOUS" ? "Previous Days" : "All Assigned"}</button>)}</nav><div className="doctor-workspace"><section className="doctor-patient-list">{loading ? <p>Loading patients…</p> : visible.length === 0 ? <div className="doctor-empty"><span>🪁</span><h2>No patients in this view</h2><p>Your assigned appointments will appear here.</p></div> : visible.map((item) => <button className={selected?.referenceId === item.referenceId ? "is-selected" : ""} type="button" onClick={() => void select(item)} key={item.referenceId}><span>🧒</span><span><strong>{item.childName}</strong><small>{item.childAge} · {item.patientId}</small><small>{item.scheduledDate} · {item.scheduledTime}</small></span><i>{item.caseHistory ? item.caseHistory.isLocked ? "COMPLETED" : "HISTORY ADDED" : "CASE NOTES DUE"}</i></button>)}</section><section className="doctor-case-panel">{!selected ? <div className="doctor-empty"><span>📋</span><h2>Select a patient</h2><p>Appointment details and case-history tools will open here.</p></div> : <><header><div><small>{selected.referenceId}</small><h2>{selected.childName}</h2><p>{selected.childAge} · Parent: {selected.parentName} · {selected.phone}</p></div><b>{selected.status}</b></header>{selected.assignmentNote && <p className="doctor-assignment-note"><b>Admin note:</b> {selected.assignmentNote}</p>}<form className="doctor-case-form" onSubmit={save}><div className="doctor-date-fields"><label>Appointment Date<input type="date" value={form.appointmentDate} onChange={(event) => setForm({ ...form, appointmentDate: event.target.value })} disabled={Boolean(selected.caseHistory)} required /></label><label>Next Appointment Date<input type="date" min={todayInput} value={form.nextAppointmentDate} onChange={(event) => setForm({ ...form, nextAppointmentDate: event.target.value })} /></label></div><div className="doctor-clinical-grid">{fields.map(([name, label, placeholder]) => <label key={name}>{label}<textarea value={form[name]} onChange={(event) => setForm({ ...form, [name]: event.target.value })} placeholder={placeholder} /></label>)}</div><label>Complete Case History<textarea className="doctor-case-main" value={form.caseHistory} onChange={(event) => setForm({ ...form, caseHistory: event.target.value })} minLength={5} placeholder="Record the child’s detailed clinical case history" required /></label><label>Additional Notes<textarea value={form.additionalNotes} onChange={(event) => setForm({ ...form, additionalNotes: event.target.value })} placeholder="Any other notes for future visits" /></label>{selected.caseHistory?.isLocked ? <p className="doctor-locked">🔒 This completed appointment is locked.</p> : <button type="submit" disabled={busy}>{busy ? "Saving…" : selected.caseHistory ? "Update Case History" : "Save Case History"}</button>}</form>{history && history.caseHistory.length > 0 && <details className="doctor-history"><summary>Previous visit history ({history.caseHistory.length})</summary>{history.caseHistory.map((visit) => <article key={visit.id}><b>Visit #{visit.appointmentNumber} · {visit.appointmentDate}</b><p>{visit.caseHistory}</p>{visit.nextAppointmentDate && <small>Next appointment: {visit.nextAppointmentDate}</small>}</article>)}</details>}</>}</section></div></main></div>;
};
